<div class="container form_layout ">
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

  <div class="row">
    <div class="col-sm-12">
    <div class="field form-group">
      <%= f.label :email %><br />
      <%= f.email_field :email, autofocus: true, class: "form-control  form-control-sm", autocomplete: "email"%>
    </div>

    <div class="field form-group">
        <%= f.label :password %><br />
        <%= f.password_field :password, class: "form-control form-control-sm",  autocomplete: "current-password" %>
    </div>

      <% if devise_mapping.rememberable? %>
        <div class="field">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me, "Mantenerme conectado" %>
        </div>
      <% end %>

      <div class="actions">
        <%= f.submit "Iniciar Sesión", class: "btn btn-primary btn-sm" %>
      </div>
      <br>
    <% end %>

      </div>
    </div>
    <%= render "organizators/shared/links" %>
  </div>
</div>
